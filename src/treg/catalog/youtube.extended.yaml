# youtube — EXTENDED tier: the provider's full endpoint surface, machine-generated by
# scripts/catalog_ingest.py. Do not hand-edit; re-run the script instead. Entries carry no
# capability, no verification and no example response — that is what the core
# youtube.yaml is for. Routes already curated there are excluded here.
# 
# Source: the youtube v3 Discovery document — the complete Data API. Paths are relative to
# base_url (youtube.googleapis.com); the three upload methods correctly point at the
# /upload/… URL rather than the metadata one, which is a 400 if you post a body to it.
# 
# COST IS QUOTA, NOT MONEY. Nothing here is billed, but a project gets 10,000 units a day and
# the methods are not equal: a list is 1 unit, a write is 50, search.list is 100, captions
# cost 200-450 and a video UPLOAD is 1600 — six uploads is more than half a day's allowance.
# Each entry's cost.note carries its published unit price (from YouTube's quota-cost table;
# the discovery document does not include it).
# 
# scope: `any_account` marks the reads that return the same public data for anyone (videos,
# search, channels, comments, playlists) — the connection is just how the call authenticates.
# Everything else acts on the connected channel.
# 
# Only 2 scope gaps, both channel memberships (members.list, membershipsLevels.list), which
# need youtube.channel-memberships.creator. The `manage` capability's four scopes cover the
# entire rest of the Data API — including the content-owner methods, which accept plain
# `youtube` as an alternative to youtubepartner and so are NOT gaps, though they are only
# meaningful to a CMS partner account.

provider: youtube
source:
  method: google discovery document
  ingested: '2026-07-28'
  spec_urls:
  - https://youtube.googleapis.com/$discovery/rest?version=v3
  - https://developers.google.com/youtube/v3/determine_quota_cost
endpoints:
- id: youtube.x.youtube-captions-insert
  kind: action
  tier: extended
  platform: youtube
  method: POST
  path: /upload/youtube/v3/captions
  name: "Upload a caption track to your YouTube video"
  summary: captions.insert — Inserts a new resource into this collection.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '400 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      onBehalfOf:
        type: string
        required: false
        note: ID of the Google+ Page for the channel that the request is be on behalf of
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
      part:
        type: string
        required: true
        note: The *part* parameter specifies the caption resource parts that the API response will include. Set the parameter value to snippet.
      sync:
        type: boolean
        required: false
        note: Extra parameter to allow automatically syncing the uploaded caption/transcript with the audio.
    bodyType: json
    body:
      type: object
      required: true
      note: JSON body — the Caption resource; see the method's API reference for its fields
- id: youtube.x.youtube-captions-update
  kind: action
  tier: extended
  platform: youtube
  method: PUT
  path: /upload/youtube/v3/captions
  name: "Update a caption track on your YouTube video"
  summary: captions.update — Updates an existing resource.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '450 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      onBehalfOf:
        type: string
        required: false
        note: ID of the Google+ Page for the channel that the request is on behalf of.
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
      part:
        type: string
        required: true
        note: The *part* parameter specifies a comma-separated list of one or more caption resource parts that the API response will include. The part names that you can include in the parameter value are id and snippet.
      sync:
        type: boolean
        required: false
        note: Extra parameter to allow automatically syncing the uploaded caption/transcript with the audio.
    bodyType: json
    body:
      type: object
      required: true
      note: JSON body — the Caption resource; see the method's API reference for its fields
- id: youtube.x.youtube-channelbanners-insert
  kind: action
  tier: extended
  platform: youtube
  method: POST
  path: /upload/youtube/v3/channelBanners/insert
  name: "Upload a banner image for your YouTube channel"
  summary: channelBanners.insert — Inserts a new resource into this collection.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      channelId:
        type: string
        required: false
        note: Unused, channel_id is currently derived from the security context of the requestor.
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
      onBehalfOfContentOwnerChannel:
        type: string
        required: false
        note: This parameter can only be used in a properly authorized request. *Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwnerChannel* parameter specifies the YouTube channel ID of the channel to which a video is being added. This parameter is required whe
    bodyType: json
    body:
      type: object
      required: true
      note: JSON body — the ChannelBannerResource resource; see the method's API reference for its fields
- id: youtube.x.youtube-playlistimages-insert
  kind: action
  tier: extended
  platform: youtube
  method: POST
  path: /upload/youtube/v3/playlistImages
  name: "Upload a cover image for your YouTube playlist"
  summary: playlistImages.insert — Inserts a new resource into this collection.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
      onBehalfOfContentOwnerChannel:
        type: string
        required: false
        note: This parameter can only be used in a properly authorized request. *Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwnerChannel* parameter specifies the YouTube channel ID of the channel to which a video is being added. This parameter is required whe
      part:
        type: string
        required: false
        note: The *part* parameter specifies the properties that the API response will include.
    bodyType: json
    body:
      type: object
      required: true
      note: JSON body — the PlaylistImage resource; see the method's API reference for its fields
- id: youtube.x.youtube-playlistimages-update
  kind: action
  tier: extended
  platform: youtube
  method: PUT
  path: /upload/youtube/v3/playlistImages
  name: "Update your YouTube playlist's cover image"
  summary: playlistImages.update — Updates an existing resource.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
      part:
        type: string
        required: false
        note: The *part* parameter specifies the properties that the API response will include.
    bodyType: json
    body:
      type: object
      required: true
      note: JSON body — the PlaylistImage resource; see the method's API reference for its fields
- id: youtube.x.youtube-watermarks-set
  kind: action
  tier: extended
  platform: youtube
  method: POST
  path: /upload/youtube/v3/watermarks/set
  name: "Upload and set your YouTube channel watermark image"
  summary: watermarks.set — Allows upload of watermark image and setting it for a channel.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      channelId:
        type: string
        required: true
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
    bodyType: json
    body:
      type: object
      required: true
      note: JSON body — the InvideoBranding resource; see the method's API reference for its fields
- id: youtube.x.youtube-abusereports-insert
  kind: action
  tier: extended
  platform: youtube
  method: POST
  path: /youtube/v3/abuseReports
  name: "File an abuse report on YouTube"
  summary: abuseReports.insert — Inserts a new resource into this collection.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      part:
        type: string
        required: true
        note: The *part* parameter serves two purposes in this operation. It identifies the properties that the write operation will set as well as the properties that the API response will include.
    bodyType: json
    body:
      type: object
      required: true
      note: JSON body — the AbuseReport resource; see the method's API reference for its fields
- id: youtube.x.youtube-activities-list
  tier: extended
  platform: youtube
  method: GET
  path: /youtube/v3/activities
  name: "List a YouTube channel's recent activities"
  summary: activities.list — Retrieves a list of resources, possibly filtered.
  scope: any_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '1 quota unit per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      channelId:
        type: string
        required: false
      home:
        type: boolean
        required: false
      maxResults:
        type: integer
        required: false
        note: The *maxResults* parameter specifies the maximum number of items that should be returned in the result set.
      mine:
        type: boolean
        required: false
      pageToken:
        type: string
        required: false
        note: The *pageToken* parameter identifies a specific page in the result set that should be returned. In an API response, the nextPageToken and prevPageToken properties identify other pages that could be retrieved.
      part:
        type: string
        required: true
        note: The *part* parameter specifies a comma-separated list of one or more activity resource properties that the API response will include. If the parameter identifies a property that contains child properties, the child properties will be included in the response. For example, in an activity resource, th
      publishedAfter:
        type: string
        required: false
      publishedBefore:
        type: string
        required: false
      regionCode:
        type: string
        required: false
- id: youtube.x.youtube-captions-delete
  kind: action
  tier: extended
  platform: youtube
  method: DELETE
  path: /youtube/v3/captions
  name: "Delete a caption track from your YouTube video"
  summary: captions.delete — Deletes a resource.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      id:
        type: string
        required: true
      onBehalfOf:
        type: string
        required: false
        note: ID of the Google+ Page for the channel that the request is be on behalf of
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
- id: youtube.x.youtube-captions-list
  tier: extended
  platform: youtube
  method: GET
  path: /youtube/v3/captions
  name: "List a YouTube video's caption tracks by video id"
  summary: captions.list — Retrieves a list of resources, possibly filtered.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      id:
        type: string
        required: false
        note: Returns the captions with the given IDs for Stubby or Apiary.
      onBehalfOf:
        type: string
        required: false
        note: ID of the Google+ Page for the channel that the request is on behalf of.
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
      part:
        type: string
        required: true
        note: The *part* parameter specifies a comma-separated list of one or more caption resource parts that the API response will include. The part names that you can include in the parameter value are id and snippet.
      videoId:
        type: string
        required: true
        note: Returns the captions for the specified video.
- id: youtube.x.youtube-captions-download
  tier: extended
  platform: youtube
  method: GET
  path: /youtube/v3/captions/{id}
  name: "Download a YouTube caption track by caption id"
  summary: captions.download — Downloads a caption track.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '200 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    pathParams:
      id:
        type: string
        required: true
        note: The ID of the caption track to download, required for One Platform.
    queryParams:
      onBehalfOf:
        type: string
        required: false
        note: ID of the Google+ Page for the channel that the request is be on behalf of
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
      tfmt:
        type: string
        required: false
        note: Convert the captions into this format. Supported options are sbv, srt, and vtt.
      tlang:
        type: string
        required: false
        note: tlang is the language code; machine translate the captions into this language.
- id: youtube.x.youtube-channelsections-delete
  kind: action
  tier: extended
  platform: youtube
  method: DELETE
  path: /youtube/v3/channelSections
  name: "Delete a section from your YouTube channel page"
  summary: channelSections.delete — Deletes a resource.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      id:
        type: string
        required: true
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
- id: youtube.x.youtube-channelsections-list
  tier: extended
  platform: youtube
  method: GET
  path: /youtube/v3/channelSections
  name: "List a YouTube channel's page sections"
  summary: channelSections.list — Retrieves a list of resources, possibly filtered.
  scope: any_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '1 quota unit per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      channelId:
        type: string
        required: false
        note: Return the ChannelSections owned by the specified channel ID.
      hl:
        type: string
        required: false
        note: Return content in specified language
      id:
        type: string
        required: false
        note: Return the ChannelSections with the given IDs for Stubby or Apiary.
      mine:
        type: boolean
        required: false
        note: Return the ChannelSections owned by the authenticated user.
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
      part:
        type: string
        required: true
        note: The *part* parameter specifies a comma-separated list of one or more channelSection resource properties that the API response will include. The part names that you can include in the parameter value are id, snippet, and contentDetails. If the parameter identifies a property that contains child prope
- id: youtube.x.youtube-channelsections-insert
  kind: action
  tier: extended
  platform: youtube
  method: POST
  path: /youtube/v3/channelSections
  name: "Add a section to your YouTube channel page"
  summary: channelSections.insert — Inserts a new resource into this collection.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
      onBehalfOfContentOwnerChannel:
        type: string
        required: false
        note: This parameter can only be used in a properly authorized request. *Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwnerChannel* parameter specifies the YouTube channel ID of the channel to which a video is being added. This parameter is required whe
      part:
        type: string
        required: true
        note: The *part* parameter serves two purposes in this operation. It identifies the properties that the write operation will set as well as the properties that the API response will include. The part names that you can include in the parameter value are snippet and contentDetails.
    bodyType: json
    body:
      type: object
      required: true
      note: JSON body — the ChannelSection resource; see the method's API reference for its fields
- id: youtube.x.youtube-channelsections-update
  kind: action
  tier: extended
  platform: youtube
  method: PUT
  path: /youtube/v3/channelSections
  name: "Update a section on your YouTube channel page"
  summary: channelSections.update — Updates an existing resource.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
      part:
        type: string
        required: true
        note: The *part* parameter serves two purposes in this operation. It identifies the properties that the write operation will set as well as the properties that the API response will include. The part names that you can include in the parameter value are snippet and contentDetails.
    bodyType: json
    body:
      type: object
      required: true
      note: JSON body — the ChannelSection resource; see the method's API reference for its fields
- id: youtube.x.youtube-channels-update
  kind: action
  tier: extended
  platform: youtube
  method: PUT
  path: /youtube/v3/channels
  name: "Update your YouTube channel settings"
  summary: channels.update — Updates an existing resource.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      onBehalfOfContentOwner:
        type: string
        required: false
        note: The *onBehalfOfContentOwner* parameter indicates that the authenticated user is acting on behalf of the content owner specified in the parameter value. This parameter is intended for YouTube content partners that own and manage many different YouTube channels. It allows content owners to authenticat
      part:
        type: string
        required: true
        note: The *part* parameter serves two purposes in this operation. It identifies the properties that the write operation will set as well as the properties that the API response will include. The API currently only allows the parameter value to be set to either brandingSettings or invideoPromotion. (You ca
    bodyType: json
    body:
      type: object
      required: true
      note: JSON body — the Channel resource; see the method's API reference for its fields
- id: youtube.x.youtube-commentthreads-insert
  kind: action
  tier: extended
  platform: youtube
  method: POST
  path: /youtube/v3/commentThreads
  name: "Post a top-level comment thread on YouTube"
  summary: commentThreads.insert — Inserts a new resource into this collection.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      part:
        type: string
        required: true
        note: The *part* parameter identifies the properties that the API response will include. Set the parameter value to snippet. The snippet part has a quota cost of 2 units.
    bodyType: json
    body:
      type: object
      required: true
      note: JSON body — the CommentThread resource; see the method's API reference for its fields
- id: youtube.x.youtube-comments-delete
  kind: action
  tier: extended
  platform: youtube
  method: DELETE
  path: /youtube/v3/comments
  name: "Delete a YouTube comment"
  summary: comments.delete — Deletes a resource.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      id:
        type: string
        required: true
- id: youtube.x.youtube-comments-list
  tier: extended
  platform: youtube
  method: GET
  path: /youtube/v3/comments
  name: "List YouTube comments by id or parent comment id"
  summary: comments.list — Retrieves a list of resources, possibly filtered.
  scope: any_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '1 quota unit per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      id:
        type: string
        required: false
        note: Returns the comments with the given IDs for One Platform.
      maxResults:
        type: integer
        required: false
        note: The *maxResults* parameter specifies the maximum number of items that should be returned in the result set.
      pageToken:
        type: string
        required: false
        note: The *pageToken* parameter identifies a specific page in the result set that should be returned. In an API response, the nextPageToken and prevPageToken properties identify other pages that could be retrieved.
      parentId:
        type: string
        required: false
        note: Returns replies to the specified comment. Note, currently YouTube features only one level of replies (ie replies to top level comments). However replies to replies may be supported in the future.
      part:
        type: string
        required: true
        note: The *part* parameter specifies a comma-separated list of one or more comment resource properties that the API response will include.
      textFormat:
        type: string
        required: false
        note: The requested text format for the returned comments.
        enum:
        - textFormatUnspecified
        - html
        - plainText
- id: youtube.x.youtube-comments-insert
  kind: action
  tier: extended
  platform: youtube
  method: POST
  path: /youtube/v3/comments
  name: "Reply to a YouTube comment"
  summary: comments.insert — Inserts a new resource into this collection.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      part:
        type: string
        required: true
        note: The *part* parameter identifies the properties that the API response will include. Set the parameter value to snippet. The snippet part has a quota cost of 2 units.
    bodyType: json
    body:
      type: object
      required: true
      note: JSON body — the Comment resource; see the method's API reference for its fields
- id: youtube.x.youtube-comments-update
  kind: action
  tier: extended
  platform: youtube
  method: PUT
  path: /youtube/v3/comments
  name: "Update your YouTube comment"
  summary: comments.update — Updates an existing resource.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      part:
        type: string
        required: true
        note: The *part* parameter identifies the properties that the API response will include. You must at least include the snippet part in the parameter value since that part contains all of the properties that the API request can update.
    bodyType: json
    body:
      type: object
      required: true
      note: JSON body — the Comment resource; see the method's API reference for its fields
- id: youtube.x.youtube-comments-markasspam
  kind: action
  tier: extended
  platform: youtube
  method: POST
  path: /youtube/v3/comments/markAsSpam
  name: "Flag YouTube comments as spam"
  summary: comments.markAsSpam — Expresses the caller's opinion that one or more comments should be flagged as spam.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      id:
        type: string
        required: true
        note: Flags the comments with the given IDs as spam in the caller's opinion.
- id: youtube.x.youtube-comments-setmoderationstatus
  kind: action
  tier: extended
  platform: youtube
  method: POST
  path: /youtube/v3/comments/setModerationStatus
  name: "Set moderation status of YouTube comments"
  summary: comments.setModerationStatus — Sets the moderation status of one or more comments.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      banAuthor:
        type: boolean
        required: false
        note: If set to true the author of the comment gets added to the ban list. This means all future comments of the author will autmomatically be rejected. Only valid in combination with STATUS_REJECTED.
      id:
        type: string
        required: true
        note: Modifies the moderation status of the comments with the given IDs
      moderationStatus:
        type: string
        required: true
        note: 'Specifies the requested moderation status. Note, comments can be in statuses, which are not available through this call. For example, this call does not allow to mark a comment as ''likely spam''. Valid values: ''heldForReview'', ''published'' or ''rejected''.'
        enum:
        - published
        - heldForReview
        - likelySpam
        - rejected
- id: youtube.x.youtube-i18nlanguages-list
  kind: utility
  tier: extended
  platform: youtube
  method: GET
  path: /youtube/v3/i18nLanguages
  name: "List YouTube interface languages"
  summary: i18nLanguages.list — Retrieves a list of resources, possibly filtered.
  scope: any_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '1 quota unit per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      hl:
        type: string
        required: false
      part:
        type: string
        required: true
        note: The *part* parameter specifies the i18nLanguage resource properties that the API response will include. Set the parameter value to snippet.
- id: youtube.x.youtube-i18nregions-list
  kind: utility
  tier: extended
  platform: youtube
  method: GET
  path: /youtube/v3/i18nRegions
  name: "List YouTube content regions"
  summary: i18nRegions.list — Retrieves a list of resources, possibly filtered.
  scope: any_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '1 quota unit per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      hl:
        type: string
        required: false
      part:
        type: string
        required: true
        note: The *part* parameter specifies the i18nRegion resource properties that the API response will include. Set the parameter value to snippet.
- id: youtube.x.youtube-livebroadcasts-delete
  kind: action
  tier: extended
  platform: youtube
  method: DELETE
  path: /youtube/v3/liveBroadcasts
  name: "Delete your YouTube live broadcast"
  summary: liveBroadcasts.delete — Delete a given broadcast.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      id:
        type: string
        required: true
        note: Broadcast to delete.
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
      onBehalfOfContentOwnerChannel:
        type: string
        required: false
        note: This parameter can only be used in a properly authorized request. *Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwnerChannel* parameter specifies the YouTube channel ID of the channel to which a video is being added. This parameter is required whe
- id: youtube.x.youtube-livebroadcasts-list
  tier: extended
  platform: youtube
  method: GET
  path: /youtube/v3/liveBroadcasts
  name: "List your YouTube live broadcasts"
  summary: liveBroadcasts.list — Retrieve the list of broadcasts associated with the given channel.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '1 quota unit per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      broadcastStatus:
        type: string
        required: false
        note: Return broadcasts with a certain status, e.g. active broadcasts.
        enum:
        - broadcastStatusFilterUnspecified
        - all
        - active
        - upcoming
        - completed
      broadcastType:
        type: string
        required: false
        note: Return only broadcasts with the selected type.
        enum:
        - broadcastTypeFilterUnspecified
        - all
        - event
        - persistent
      id:
        type: string
        required: false
        note: Return broadcasts with the given ids from Stubby or Apiary.
      maxResults:
        type: integer
        required: false
        note: The *maxResults* parameter specifies the maximum number of items that should be returned in the result set.
      mine:
        type: boolean
        required: false
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
      onBehalfOfContentOwnerChannel:
        type: string
        required: false
        note: This parameter can only be used in a properly authorized request. *Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwnerChannel* parameter specifies the YouTube channel ID of the channel to which a video is being added. This parameter is required whe
      pageToken:
        type: string
        required: false
        note: The *pageToken* parameter identifies a specific page in the result set that should be returned. In an API response, the nextPageToken and prevPageToken properties identify other pages that could be retrieved.
      part:
        type: string
        required: true
        note: The *part* parameter specifies a comma-separated list of one or more liveBroadcast resource properties that the API response will include. The part names that you can include in the parameter value are id, snippet, contentDetails, status and statistics.
- id: youtube.x.youtube-livebroadcasts-insert
  kind: action
  tier: extended
  platform: youtube
  method: POST
  path: /youtube/v3/liveBroadcasts
  name: "Create a YouTube live broadcast"
  summary: liveBroadcasts.insert — Inserts a new stream for the authenticated user.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
      onBehalfOfContentOwnerChannel:
        type: string
        required: false
        note: This parameter can only be used in a properly authorized request. *Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwnerChannel* parameter specifies the YouTube channel ID of the channel to which a video is being added. This parameter is required whe
      part:
        type: string
        required: true
        note: The *part* parameter serves two purposes in this operation. It identifies the properties that the write operation will set as well as the properties that the API response will include. The part properties that you can include in the parameter value are id, snippet, contentDetails, and status.
    bodyType: json
    body:
      type: object
      required: true
      note: JSON body — the LiveBroadcast resource; see the method's API reference for its fields
- id: youtube.x.youtube-livebroadcasts-update
  kind: action
  tier: extended
  platform: youtube
  method: PUT
  path: /youtube/v3/liveBroadcasts
  name: "Update your YouTube live broadcast settings"
  summary: liveBroadcasts.update — Updates an existing broadcast for the authenticated user.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
      onBehalfOfContentOwnerChannel:
        type: string
        required: false
        note: This parameter can only be used in a properly authorized request. *Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwnerChannel* parameter specifies the YouTube channel ID of the channel to which a video is being added. This parameter is required whe
      part:
        type: string
        required: true
        note: The *part* parameter serves two purposes in this operation. It identifies the properties that the write operation will set as well as the properties that the API response will include. The part properties that you can include in the parameter value are id, snippet, contentDetails, and status. Note t
    bodyType: json
    body:
      type: object
      required: true
      note: JSON body — the LiveBroadcast resource; see the method's API reference for its fields
- id: youtube.x.youtube-livebroadcasts-bind
  kind: action
  tier: extended
  platform: youtube
  method: POST
  path: /youtube/v3/liveBroadcasts/bind
  name: "Bind a YouTube live broadcast to a stream"
  summary: liveBroadcasts.bind — Bind a broadcast to a stream.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      id:
        type: string
        required: true
        note: Broadcast to bind to the stream
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
      onBehalfOfContentOwnerChannel:
        type: string
        required: false
        note: This parameter can only be used in a properly authorized request. *Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwnerChannel* parameter specifies the YouTube channel ID of the channel to which a video is being added. This parameter is required whe
      part:
        type: string
        required: true
        note: The *part* parameter specifies a comma-separated list of one or more liveBroadcast resource properties that the API response will include. The part names that you can include in the parameter value are id, snippet, contentDetails, and status.
      streamId:
        type: string
        required: false
        note: Stream to bind, if not set unbind the current one.
- id: youtube.x.youtube-livebroadcasts-insertcuepoint
  kind: action
  tier: extended
  platform: youtube
  method: POST
  path: /youtube/v3/liveBroadcasts/cuepoint
  name: "Insert a cuepoint into your YouTube live broadcast"
  summary: liveBroadcasts.insertCuepoint — Insert cuepoints in a broadcast
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      id:
        type: string
        required: false
        note: Broadcast to insert ads to, or equivalently `external_video_id` for internal use.
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
      onBehalfOfContentOwnerChannel:
        type: string
        required: false
        note: This parameter can only be used in a properly authorized request. *Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwnerChannel* parameter specifies the YouTube channel ID of the channel to which a video is being added. This parameter is required whe
      part:
        type: string
        required: false
        note: The *part* parameter specifies a comma-separated list of one or more liveBroadcast resource properties that the API response will include. The part names that you can include in the parameter value are id, snippet, contentDetails, and status.
    bodyType: json
    body:
      type: object
      required: true
      note: JSON body — the Cuepoint resource; see the method's API reference for its fields
- id: youtube.x.youtube-livebroadcasts-transition
  kind: action
  tier: extended
  platform: youtube
  method: POST
  path: /youtube/v3/liveBroadcasts/transition
  name: "Transition your YouTube live broadcast status"
  summary: liveBroadcasts.transition — Transition a broadcast to a given status.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      broadcastStatus:
        type: string
        required: true
        note: The status to which the broadcast is going to transition.
        enum:
        - statusUnspecified
        - testing
        - live
        - complete
      id:
        type: string
        required: true
        note: Broadcast to transition.
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
      onBehalfOfContentOwnerChannel:
        type: string
        required: false
        note: This parameter can only be used in a properly authorized request. *Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwnerChannel* parameter specifies the YouTube channel ID of the channel to which a video is being added. This parameter is required whe
      part:
        type: string
        required: true
        note: The *part* parameter specifies a comma-separated list of one or more liveBroadcast resource properties that the API response will include. The part names that you can include in the parameter value are id, snippet, contentDetails, and status.
- id: youtube.x.youtube-livechatbans-delete
  kind: action
  tier: extended
  platform: youtube
  method: DELETE
  path: /youtube/v3/liveChat/bans
  name: "Lift a YouTube live chat ban"
  summary: liveChatBans.delete — Deletes a chat ban.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      id:
        type: string
        required: true
- id: youtube.x.youtube-livechatbans-insert
  kind: action
  tier: extended
  platform: youtube
  method: POST
  path: /youtube/v3/liveChat/bans
  name: "Ban a user from your YouTube live chat"
  summary: liveChatBans.insert — Inserts a new resource into this collection.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      part:
        type: string
        required: true
        note: The *part* parameter serves two purposes in this operation. It identifies the properties that the write operation will set as well as the properties that the API response returns. Set the parameter value to snippet.
    bodyType: json
    body:
      type: object
      required: true
      note: JSON body — the LiveChatBan resource; see the method's API reference for its fields
- id: youtube.x.youtube-livechatmessages-delete
  kind: action
  tier: extended
  platform: youtube
  method: DELETE
  path: /youtube/v3/liveChat/messages
  name: "Delete a YouTube live chat message"
  summary: liveChatMessages.delete — Deletes a chat message.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      id:
        type: string
        required: true
- id: youtube.x.youtube-livechatmessages-list
  tier: extended
  platform: youtube
  method: GET
  path: /youtube/v3/liveChat/messages
  name: "List YouTube live chat messages by chat id"
  summary: liveChatMessages.list — Retrieves a list of resources, possibly filtered.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '1 quota unit per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      hl:
        type: string
        required: false
        note: Specifies the localization language in which the system messages should be returned.
      liveChatId:
        type: string
        required: true
        note: The id of the live chat for which comments should be returned.
      maxResults:
        type: integer
        required: false
        note: The *maxResults* parameter specifies the maximum number of items that should be returned in the result set. Not used in the streaming RPC.
      pageToken:
        type: string
        required: false
        note: The *pageToken* parameter identifies a specific page in the result set that should be returned. In an API response, the nextPageToken property identify other pages that could be retrieved.
      part:
        type: string
        required: true
        note: The *part* parameter specifies the liveChatComment resource parts that the API response will include. Supported values are id, snippet, and authorDetails.
      profileImageSize:
        type: integer
        required: false
        note: Specifies the size of the profile image that should be returned for each user.
- id: youtube.x.youtube-livechatmessages-insert
  kind: action
  tier: extended
  platform: youtube
  method: POST
  path: /youtube/v3/liveChat/messages
  name: "Post a message to a YouTube live chat"
  summary: liveChatMessages.insert — Inserts a new resource into this collection.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      part:
        type: string
        required: true
        note: The *part* parameter serves two purposes. It identifies the properties that the write operation will set as well as the properties that the API response will include. Set the parameter value to snippet.
    bodyType: json
    body:
      type: object
      required: true
      note: JSON body — the LiveChatMessage resource; see the method's API reference for its fields
- id: youtube.x.youtube-youtube-v3-livechat-messages-stream
  tier: extended
  platform: youtube
  method: GET
  path: /youtube/v3/liveChat/messages/stream
  name: "Stream YouTube live chat messages by chat id"
  summary: messages.stream — Allows a user to load live chat through a server-streamed RPC.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      hl:
        type: string
        required: false
        note: Specifies the localization language in which the system messages should be returned.
      liveChatId:
        type: string
        required: false
        note: The id of the live chat for which comments should be returned.
      maxResults:
        type: integer
        required: false
        note: The *maxResults* parameter specifies the maximum number of items that should be returned in the result set. Not used in the streaming RPC.
      pageToken:
        type: string
        required: false
        note: The *pageToken* parameter identifies a specific page in the result set that should be returned. In an API response, the nextPageToken property identify other pages that could be retrieved.
      part:
        type: string
        required: false
        note: The *part* parameter specifies the liveChatComment resource parts that the API response will include. Supported values are id, snippet, and authorDetails.
      profileImageSize:
        type: integer
        required: false
        note: Specifies the size of the profile image that should be returned for each user.
- id: youtube.x.youtube-livechatmessages-transition
  kind: action
  tier: extended
  platform: youtube
  method: POST
  path: /youtube/v3/liveChat/messages/transition
  name: "Transition a YouTube live chat event"
  summary: liveChatMessages.transition — Transition a durable chat event.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      id:
        type: string
        required: false
        note: The ID that uniquely identify the chat message event to transition.
      status:
        type: string
        required: false
        note: The status to which the chat event is going to transition.
        enum:
        - statusUnspecified
        - closed
- id: youtube.x.youtube-livechatmoderators-delete
  kind: action
  tier: extended
  platform: youtube
  method: DELETE
  path: /youtube/v3/liveChat/moderators
  name: "Remove a moderator from your YouTube live chat"
  summary: liveChatModerators.delete — Deletes a chat moderator.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      id:
        type: string
        required: true
- id: youtube.x.youtube-livechatmoderators-list
  tier: extended
  platform: youtube
  method: GET
  path: /youtube/v3/liveChat/moderators
  name: "List moderators of your YouTube live chat"
  summary: liveChatModerators.list — Retrieves a list of resources, possibly filtered.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '1 quota unit per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      liveChatId:
        type: string
        required: true
        note: The id of the live chat for which moderators should be returned.
      maxResults:
        type: integer
        required: false
        note: The *maxResults* parameter specifies the maximum number of items that should be returned in the result set.
      pageToken:
        type: string
        required: false
        note: The *pageToken* parameter identifies a specific page in the result set that should be returned. In an API response, the nextPageToken and prevPageToken properties identify other pages that could be retrieved.
      part:
        type: string
        required: true
        note: The *part* parameter specifies the liveChatModerator resource parts that the API response will include. Supported values are id and snippet.
- id: youtube.x.youtube-livechatmoderators-insert
  kind: action
  tier: extended
  platform: youtube
  method: POST
  path: /youtube/v3/liveChat/moderators
  name: "Add a moderator to your YouTube live chat"
  summary: liveChatModerators.insert — Inserts a new resource into this collection.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      part:
        type: string
        required: true
        note: The *part* parameter serves two purposes in this operation. It identifies the properties that the write operation will set as well as the properties that the API response returns. Set the parameter value to snippet.
    bodyType: json
    body:
      type: object
      required: true
      note: JSON body — the LiveChatModerator resource; see the method's API reference for its fields
- id: youtube.x.youtube-livestreams-delete
  kind: action
  tier: extended
  platform: youtube
  method: DELETE
  path: /youtube/v3/liveStreams
  name: "Delete your YouTube live stream"
  summary: liveStreams.delete — Deletes an existing stream for the authenticated user.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      id:
        type: string
        required: true
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
      onBehalfOfContentOwnerChannel:
        type: string
        required: false
        note: This parameter can only be used in a properly authorized request. *Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwnerChannel* parameter specifies the YouTube channel ID of the channel to which a video is being added. This parameter is required whe
- id: youtube.x.youtube-livestreams-list
  tier: extended
  platform: youtube
  method: GET
  path: /youtube/v3/liveStreams
  name: "List your YouTube live streams"
  summary: liveStreams.list — Retrieve the list of streams associated with the given channel. --
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '1 quota unit per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      id:
        type: string
        required: false
        note: Return LiveStreams with the given ids from Stubby or Apiary.
      maxResults:
        type: integer
        required: false
        note: The *maxResults* parameter specifies the maximum number of items that should be returned in the result set.
      mine:
        type: boolean
        required: false
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
      onBehalfOfContentOwnerChannel:
        type: string
        required: false
        note: This parameter can only be used in a properly authorized request. *Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwnerChannel* parameter specifies the YouTube channel ID of the channel to which a video is being added. This parameter is required whe
      pageToken:
        type: string
        required: false
        note: The *pageToken* parameter identifies a specific page in the result set that should be returned. In an API response, the nextPageToken and prevPageToken properties identify other pages that could be retrieved.
      part:
        type: string
        required: true
        note: The *part* parameter specifies a comma-separated list of one or more liveStream resource properties that the API response will include. The part names that you can include in the parameter value are id, snippet, cdn, and status.
- id: youtube.x.youtube-livestreams-insert
  kind: action
  tier: extended
  platform: youtube
  method: POST
  path: /youtube/v3/liveStreams
  name: "Create a YouTube live stream"
  summary: liveStreams.insert — Inserts a new stream for the authenticated user.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
      onBehalfOfContentOwnerChannel:
        type: string
        required: false
        note: This parameter can only be used in a properly authorized request. *Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwnerChannel* parameter specifies the YouTube channel ID of the channel to which a video is being added. This parameter is required whe
      part:
        type: string
        required: true
        note: The *part* parameter serves two purposes in this operation. It identifies the properties that the write operation will set as well as the properties that the API response will include. The part properties that you can include in the parameter value are id, snippet, cdn, content_details, and status.
    bodyType: json
    body:
      type: object
      required: true
      note: JSON body — the LiveStream resource; see the method's API reference for its fields
- id: youtube.x.youtube-livestreams-update
  kind: action
  tier: extended
  platform: youtube
  method: PUT
  path: /youtube/v3/liveStreams
  name: "Update your YouTube live stream settings"
  summary: liveStreams.update — Updates an existing stream for the authenticated user.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
      onBehalfOfContentOwnerChannel:
        type: string
        required: false
        note: This parameter can only be used in a properly authorized request. *Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwnerChannel* parameter specifies the YouTube channel ID of the channel to which a video is being added. This parameter is required whe
      part:
        type: string
        required: true
        note: The *part* parameter serves two purposes in this operation. It identifies the properties that the write operation will set as well as the properties that the API response will include. The part properties that you can include in the parameter value are id, snippet, cdn, and status. Note that this me
    bodyType: json
    body:
      type: object
      required: true
      note: JSON body — the LiveStream resource; see the method's API reference for its fields
- id: youtube.x.youtube-members-list
  tier: extended
  platform: youtube
  method: GET
  path: /youtube/v3/members
  name: "List a YouTube channel's members"
  summary: members.list — Retrieves a list of members that match the request criteria for a channel.
  scope: any_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '1 quota unit per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  scope_gap: 'treg''s OAuth app does not request this; needs one of: youtube.channel-memberships.creator'
  input:
    queryParams:
      filterByMemberChannelId:
        type: string
        required: false
        note: Comma separated list of channel IDs. Only data about members that are part of this list will be included in the response.
      hasAccessToLevel:
        type: string
        required: false
        note: Filter members in the results set to the ones that have access to a level.
      maxResults:
        type: integer
        required: false
        note: The *maxResults* parameter specifies the maximum number of items that should be returned in the result set.
      mode:
        type: string
        required: false
        note: Parameter that specifies which channel members to return.
        enum:
        - listMembersModeUnknown
        - updates
        - all_current
      pageToken:
        type: string
        required: false
        note: The *pageToken* parameter identifies a specific page in the result set that should be returned. In an API response, the nextPageToken and prevPageToken properties identify other pages that could be retrieved.
      part:
        type: string
        required: true
        note: The *part* parameter specifies the member resource parts that the API response will include. Set the parameter value to snippet.
- id: youtube.x.youtube-membershipslevels-list
  tier: extended
  platform: youtube
  method: GET
  path: /youtube/v3/membershipsLevels
  name: "List your YouTube membership pricing levels"
  summary: membershipsLevels.list — Retrieves a list of all pricing levels offered by a creator to the fans.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '1 quota unit per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  scope_gap: 'treg''s OAuth app does not request this; needs one of: youtube.channel-memberships.creator'
  input:
    queryParams:
      part:
        type: string
        required: true
        note: The *part* parameter specifies the membershipsLevel resource parts that the API response will include. Supported values are id and snippet.
- id: youtube.x.youtube-playlistimages-delete
  kind: action
  tier: extended
  platform: youtube
  method: DELETE
  path: /youtube/v3/playlistImages
  name: "Delete your YouTube playlist's cover image"
  summary: playlistImages.delete — Deletes a resource.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      id:
        type: string
        required: false
        note: Id to identify this image. This is returned from by the List method.
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
- id: youtube.x.youtube-playlistimages-list
  tier: extended
  platform: youtube
  method: GET
  path: /youtube/v3/playlistImages
  name: "List a YouTube playlist's cover images"
  summary: playlistImages.list — Retrieves a list of resources, possibly filtered.
  scope: any_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '1 quota unit per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      maxResults:
        type: integer
        required: false
        note: The *maxResults* parameter specifies the maximum number of items that should be returned in the result set.
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
      onBehalfOfContentOwnerChannel:
        type: string
        required: false
        note: This parameter can only be used in a properly authorized request. *Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwnerChannel* parameter specifies the YouTube channel ID of the channel to which a video is being added. This parameter is required whe
      pageToken:
        type: string
        required: false
        note: The *pageToken* parameter identifies a specific page in the result set that should be returned. In an API response, the nextPageToken and prevPageToken properties identify other pages that could be retrieved.
      parent:
        type: string
        required: false
        note: Return PlaylistImages for this playlist id.
      part:
        type: string
        required: false
        note: The *part* parameter specifies a comma-separated list of one or more playlistImage resource properties that the API response will include. If the parameter identifies a property that contains child properties, the child properties will be included in the response.
- id: youtube.x.youtube-playlistitems-delete
  kind: action
  tier: extended
  platform: youtube
  method: DELETE
  path: /youtube/v3/playlistItems
  name: "Remove a video from your YouTube playlist"
  summary: playlistItems.delete — Deletes a resource.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      id:
        type: string
        required: true
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
- id: youtube.x.youtube-playlistitems-insert
  kind: action
  tier: extended
  platform: youtube
  method: POST
  path: /youtube/v3/playlistItems
  name: "Add a video to your YouTube playlist"
  summary: playlistItems.insert — Inserts a new resource into this collection.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
      part:
        type: string
        required: true
        note: The *part* parameter serves two purposes in this operation. It identifies the properties that the write operation will set as well as the properties that the API response will include.
    bodyType: json
    body:
      type: object
      required: true
      note: JSON body — the PlaylistItem resource; see the method's API reference for its fields
- id: youtube.x.youtube-playlistitems-update
  kind: action
  tier: extended
  platform: youtube
  method: PUT
  path: /youtube/v3/playlistItems
  name: "Update a video entry in your YouTube playlist"
  summary: playlistItems.update — Updates an existing resource.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
      part:
        type: string
        required: true
        note: The *part* parameter serves two purposes in this operation. It identifies the properties that the write operation will set as well as the properties that the API response will include. Note that this method will override the existing values for all of the mutable properties that are contained in any
    bodyType: json
    body:
      type: object
      required: true
      note: JSON body — the PlaylistItem resource; see the method's API reference for its fields
- id: youtube.x.youtube-playlists-delete
  kind: action
  tier: extended
  platform: youtube
  method: DELETE
  path: /youtube/v3/playlists
  name: "Delete your YouTube playlist"
  summary: playlists.delete — Deletes a resource.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      id:
        type: string
        required: true
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
- id: youtube.x.youtube-playlists-list
  tier: extended
  platform: youtube
  method: GET
  path: /youtube/v3/playlists
  name: "List a channel's YouTube playlists"
  summary: playlists.list — Retrieves a list of resources, possibly filtered.
  scope: any_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '1 quota unit per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      channelId:
        type: string
        required: false
        note: Return the playlists owned by the specified channel ID.
      hl:
        type: string
        required: false
        note: Return content in specified language
      id:
        type: string
        required: false
        note: Return the playlists with the given IDs for Stubby or Apiary.
      maxResults:
        type: integer
        required: false
        note: The *maxResults* parameter specifies the maximum number of items that should be returned in the result set.
      mine:
        type: boolean
        required: false
        note: Return the playlists owned by the authenticated user.
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
      onBehalfOfContentOwnerChannel:
        type: string
        required: false
        note: This parameter can only be used in a properly authorized request. *Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwnerChannel* parameter specifies the YouTube channel ID of the channel to which a video is being added. This parameter is required whe
      pageToken:
        type: string
        required: false
        note: The *pageToken* parameter identifies a specific page in the result set that should be returned. In an API response, the nextPageToken and prevPageToken properties identify other pages that could be retrieved.
      part:
        type: string
        required: true
        note: The *part* parameter specifies a comma-separated list of one or more playlist resource properties that the API response will include. If the parameter identifies a property that contains child properties, the child properties will be included in the response. For example, in a playlist resource, the
- id: youtube.x.youtube-playlists-insert
  kind: action
  tier: extended
  platform: youtube
  method: POST
  path: /youtube/v3/playlists
  name: "Create a YouTube playlist"
  summary: playlists.insert — Inserts a new resource into this collection.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
      onBehalfOfContentOwnerChannel:
        type: string
        required: false
        note: This parameter can only be used in a properly authorized request. *Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwnerChannel* parameter specifies the YouTube channel ID of the channel to which a video is being added. This parameter is required whe
      part:
        type: string
        required: true
        note: The *part* parameter serves two purposes in this operation. It identifies the properties that the write operation will set as well as the properties that the API response will include.
    bodyType: json
    body:
      type: object
      required: true
      note: JSON body — the Playlist resource; see the method's API reference for its fields
- id: youtube.x.youtube-playlists-update
  kind: action
  tier: extended
  platform: youtube
  method: PUT
  path: /youtube/v3/playlists
  name: "Update your YouTube playlist details"
  summary: playlists.update — Updates an existing resource.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
      part:
        type: string
        required: true
        note: The *part* parameter serves two purposes in this operation. It identifies the properties that the write operation will set as well as the properties that the API response will include. Note that this method will override the existing values for mutable properties that are contained in any parts that
    bodyType: json
    body:
      type: object
      required: true
      note: JSON body — the Playlist resource; see the method's API reference for its fields
- id: youtube.x.youtube-subscriptions-delete
  kind: action
  tier: extended
  platform: youtube
  method: DELETE
  path: /youtube/v3/subscriptions
  name: "Unsubscribe from a YouTube channel"
  summary: subscriptions.delete — Deletes a resource.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      id:
        type: string
        required: true
- id: youtube.x.youtube-subscriptions-list
  tier: extended
  platform: youtube
  method: GET
  path: /youtube/v3/subscriptions
  name: "List YouTube channel subscriptions"
  summary: subscriptions.list — Retrieves a list of resources, possibly filtered.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '1 quota unit per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      channelId:
        type: string
        required: false
        note: Return the subscriptions of the given channel owner.
      forChannelId:
        type: string
        required: false
        note: Return the subscriptions to the subset of these channels that the authenticated user is subscribed to.
      id:
        type: string
        required: false
        note: Return the subscriptions with the given IDs for Stubby or Apiary.
      maxResults:
        type: integer
        required: false
        note: The *maxResults* parameter specifies the maximum number of items that should be returned in the result set.
      mine:
        type: boolean
        required: false
        note: Flag for returning the subscriptions of the authenticated user.
      myRecentSubscribers:
        type: boolean
        required: false
      mySubscribers:
        type: boolean
        required: false
        note: Return the subscribers of the given channel owner.
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
      onBehalfOfContentOwnerChannel:
        type: string
        required: false
        note: This parameter can only be used in a properly authorized request. *Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwnerChannel* parameter specifies the YouTube channel ID of the channel to which a video is being added. This parameter is required whe
      order:
        type: string
        required: false
        note: The order of the returned subscriptions
        enum:
        - subscriptionOrderUnspecified
        - relevance
        - unread
        - alphabetical
      pageToken:
        type: string
        required: false
        note: The *pageToken* parameter identifies a specific page in the result set that should be returned. In an API response, the nextPageToken and prevPageToken properties identify other pages that could be retrieved.
      part:
        type: string
        required: true
        note: The *part* parameter specifies a comma-separated list of one or more subscription resource properties that the API response will include. If the parameter identifies a property that contains child properties, the child properties will be included in the response. For example, in a subscription resou
- id: youtube.x.youtube-subscriptions-insert
  kind: action
  tier: extended
  platform: youtube
  method: POST
  path: /youtube/v3/subscriptions
  name: "Subscribe to a YouTube channel"
  summary: subscriptions.insert — Inserts a new resource into this collection.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      part:
        type: string
        required: true
        note: The *part* parameter serves two purposes in this operation. It identifies the properties that the write operation will set as well as the properties that the API response will include.
    bodyType: json
    body:
      type: object
      required: true
      note: JSON body — the Subscription resource; see the method's API reference for its fields
- id: youtube.x.youtube-superchatevents-list
  tier: extended
  platform: youtube
  method: GET
  path: /youtube/v3/superChatEvents
  name: "List Super Chat events on your YouTube channel"
  summary: superChatEvents.list — Retrieves a list of resources, possibly filtered.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '1 quota unit per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      hl:
        type: string
        required: false
        note: Return rendered funding amounts in specified language.
      maxResults:
        type: integer
        required: false
        note: The *maxResults* parameter specifies the maximum number of items that should be returned in the result set.
      pageToken:
        type: string
        required: false
        note: The *pageToken* parameter identifies a specific page in the result set that should be returned. In an API response, the nextPageToken and prevPageToken properties identify other pages that could be retrieved.
      part:
        type: string
        required: true
        note: The *part* parameter specifies the superChatEvent resource parts that the API response will include. This parameter is currently not supported.
- id: youtube.x.youtube-tests-insert
  kind: action
  tier: extended
  platform: youtube
  method: POST
  path: /youtube/v3/tests
  name: "Run a YouTube connectivity test call"
  summary: tests.insert — POST method.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      externalChannelId:
        type: string
        required: false
      onBehalfOfContentOwnerChannel:
        type: string
        required: false
      part:
        type: string
        required: true
    bodyType: json
    body:
      type: object
      required: true
      note: JSON body — the TestItem resource; see the method's API reference for its fields
- id: youtube.x.youtube-thirdpartylinks-delete
  kind: action
  tier: extended
  platform: youtube
  method: DELETE
  path: /youtube/v3/thirdPartyLinks
  name: "Delete a third-party link from your YouTube channel"
  summary: thirdPartyLinks.delete — Deletes a resource.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      externalChannelId:
        type: string
        required: false
        note: Channel ID to which changes should be applied, for delegation.
      linkingToken:
        type: string
        required: true
        note: Delete the partner links with the given linking token.
      part:
        type: string
        required: false
        note: Do not use. Required for compatibility.
      type:
        type: string
        required: true
        note: Type of the link to be deleted.
        enum:
        - linkUnspecified
        - channelToStoreLink
        - channelToAffiliateProgramLink
- id: youtube.x.youtube-thirdpartylinks-list
  tier: extended
  platform: youtube
  method: GET
  path: /youtube/v3/thirdPartyLinks
  name: "List third-party links on your YouTube channel"
  summary: thirdPartyLinks.list — Retrieves a list of resources, possibly filtered.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '1 quota unit per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      externalChannelId:
        type: string
        required: false
        note: Channel ID to which changes should be applied, for delegation.
      linkingToken:
        type: string
        required: false
        note: Get a third party link with the given linking token.
      part:
        type: string
        required: true
        note: The *part* parameter specifies the thirdPartyLink resource parts that the API response will include. Supported values are linkingToken, status, and snippet.
      type:
        type: string
        required: false
        note: Get a third party link of the given type.
        enum:
        - linkUnspecified
        - channelToStoreLink
        - channelToAffiliateProgramLink
- id: youtube.x.youtube-thirdpartylinks-insert
  kind: action
  tier: extended
  platform: youtube
  method: POST
  path: /youtube/v3/thirdPartyLinks
  name: "Create a third-party link on your YouTube channel"
  summary: thirdPartyLinks.insert — Inserts a new resource into this collection.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      externalChannelId:
        type: string
        required: false
        note: Channel ID to which changes should be applied, for delegation.
      part:
        type: string
        required: true
        note: The *part* parameter specifies the thirdPartyLink resource parts that the API request and response will include. Supported values are linkingToken, status, and snippet.
    bodyType: json
    body:
      type: object
      required: true
      note: JSON body — the ThirdPartyLink resource; see the method's API reference for its fields
- id: youtube.x.youtube-thirdpartylinks-update
  kind: action
  tier: extended
  platform: youtube
  method: PUT
  path: /youtube/v3/thirdPartyLinks
  name: "Update a third-party link on your YouTube channel"
  summary: thirdPartyLinks.update — Updates an existing resource.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      externalChannelId:
        type: string
        required: false
        note: Channel ID to which changes should be applied, for delegation.
      part:
        type: string
        required: true
        note: The *part* parameter specifies the thirdPartyLink resource parts that the API request and response will include. Supported values are linkingToken, status, and snippet.
    bodyType: json
    body:
      type: object
      required: true
      note: JSON body — the ThirdPartyLink resource; see the method's API reference for its fields
- id: youtube.x.youtube-videoabusereportreasons-list
  kind: utility
  tier: extended
  platform: youtube
  method: GET
  path: /youtube/v3/videoAbuseReportReasons
  name: "List YouTube video abuse report reasons"
  summary: videoAbuseReportReasons.list — Retrieves a list of resources, possibly filtered.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '1 quota unit per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      hl:
        type: string
        required: false
      part:
        type: string
        required: true
        note: The *part* parameter specifies the videoCategory resource parts that the API response will include. Supported values are id and snippet.
- id: youtube.x.youtube-videocategories-list
  kind: utility
  tier: extended
  platform: youtube
  method: GET
  path: /youtube/v3/videoCategories
  name: "List YouTube video categories by region"
  summary: videoCategories.list — Retrieves a list of resources, possibly filtered.
  scope: any_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '1 quota unit per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      hl:
        type: string
        required: false
      id:
        type: string
        required: false
        note: Returns the video categories with the given IDs for Stubby or Apiary.
      part:
        type: string
        required: true
        note: The *part* parameter specifies the videoCategory resource properties that the API response will include. Set the parameter value to snippet.
      regionCode:
        type: string
        required: false
- id: youtube.x.youtube-videotrainability-get
  tier: extended
  platform: youtube
  method: GET
  path: /youtube/v3/videoTrainability
  name: "Check a YouTube video's trainability status"
  summary: videoTrainability.get — Returns the trainability status of a video.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      id:
        type: string
        required: false
        note: The ID of the video to retrieve.
- id: youtube.x.youtube-videos-delete
  kind: action
  tier: extended
  platform: youtube
  method: DELETE
  path: /youtube/v3/videos
  name: "Delete your YouTube video"
  summary: videos.delete — Deletes a resource.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      id:
        type: string
        required: true
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
- id: youtube.x.youtube-videos-update
  kind: action
  tier: extended
  platform: youtube
  method: PUT
  path: /youtube/v3/videos
  name: "Update your YouTube video's metadata"
  summary: videos.update — Updates an existing resource.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
      part:
        type: string
        required: true
        note: The *part* parameter serves two purposes in this operation. It identifies the properties that the write operation will set as well as the properties that the API response will include. Note that this method will override the existing values for all of the mutable properties that are contained in any
    bodyType: json
    body:
      type: object
      required: true
      note: JSON body — the Video resource; see the method's API reference for its fields
- id: youtube.x.youtube-videos-getrating
  tier: extended
  platform: youtube
  method: GET
  path: /youtube/v3/videos/getRating
  name: "Your ratings on YouTube videos by video id"
  summary: videos.getRating — Retrieves the ratings that the authorized user gave to a list of specified videos.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '1 quota unit per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      id:
        type: string
        required: true
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
- id: youtube.x.youtube-videos-rate
  kind: action
  tier: extended
  platform: youtube
  method: POST
  path: /youtube/v3/videos/rate
  name: "Like, dislike or unrate a YouTube video"
  summary: videos.rate — Adds a like or dislike rating to a video or removes a rating from a video.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      id:
        type: string
        required: true
      rating:
        type: string
        required: true
        enum:
        - none
        - like
        - dislike
- id: youtube.x.youtube-videos-reportabuse
  kind: action
  tier: extended
  platform: youtube
  method: POST
  path: /youtube/v3/videos/reportAbuse
  name: "Report a YouTube video for abuse"
  summary: videos.reportAbuse — Report abuse for a video.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
    bodyType: json
    body:
      type: object
      required: true
      note: JSON body — the VideoAbuseReport resource; see the method's API reference for its fields
- id: youtube.x.youtube-videos-batchgetstats
  tier: extended
  platform: youtube
  method: GET
  path: /youtube/v3/videos:batchGetStats
  name: "Batch stats for your YouTube videos by video ids"
  summary: videos.batchGetStats — Retrieves a batch of VideoStat resources, possibly filtered. BatchGetStats is intentionally not atomic to provide a better user experience.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      id:
        type: string
        required: false
        note: Required. Return videos with the given ids. The number of IDs specified cannot exceed 50.
      onBehalfOfContentOwner:
        type: string
        required: false
        note: Optional. **Note:** This parameter is intended exclusively for YouTube content partners. The `onBehalfOfContentOwner` parameter indicates that the request's authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This param
      part:
        type: string
        required: false
        note: Required. The `**part**` parameter specifies a comma-separated list of one or more `videoStat` resource properties that the API response will include. If the parameter identifies a property that contains child properties, the child properties will be included in the response. For example, in a `vide
- id: youtube.x.youtube-watermarks-unset
  kind: action
  tier: extended
  platform: youtube
  method: POST
  path: /youtube/v3/watermarks/unset
  name: "Remove your YouTube channel's watermark image"
  summary: watermarks.unset — Allows removal of channel watermark.
  scope: own_account
  cost:
    type: free
    value: 0
    currency: USD
    unit: call
    note: '50 quota units per call (default project allowance: 10,000 units/day)'
  docs_url: https://developers.google.com/youtube/v3/docs
  input:
    queryParams:
      channelId:
        type: string
        required: true
      onBehalfOfContentOwner:
        type: string
        required: false
        note: '*Note:* This parameter is intended exclusively for YouTube content partners. The *onBehalfOfContentOwner* parameter indicates that the request''s authorization credentials identify a YouTube CMS user who is acting on behalf of the content owner specified in the parameter value. This parameter is inte'
